# Workspace

- `to_be_extracted/`: normalized source Markdown files created from OpenBB data
- `extractions/`: Preciso-compatible extraction JSON files generated by the agent
- `manifests/`: provenance manifests for each generated source artifact

These directories are created automatically at runtime.
